# 36. A target site has no protective measures; uploaded images can be accessed normally, but accessing uploaded script formats results in a 403 error. What might be the reason?

## Likely Reason
The web server configuration likely restricts the upload directory from executing scripts. One can attempt to bypass by changing the file extension.

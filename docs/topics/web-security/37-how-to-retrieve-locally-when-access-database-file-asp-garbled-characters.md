# 37. When an Access database file with an `.asp` extension is discovered and displays garbled characters upon access, how can it be retrieved locally for use?

## Method
Use a download manager (e.g., Thunder) to download the file, then change the extension to `.mdb`.

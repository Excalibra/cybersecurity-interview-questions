# Q: What is a virtual machine escape?

**A:** Virtual machine escape is a security vulnerability where an attacker exploits a flaw in the virtualisation software or the guest operating system to break out of a virtual machine (VM) environment and gain control of the physical host. This bypasses the security isolation that virtualisation provides, giving the attacker access to the host and all other VMs running on it.

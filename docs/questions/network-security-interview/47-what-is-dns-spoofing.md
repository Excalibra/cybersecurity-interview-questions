# Q: What is DNS spoofing?

**A:** DNS spoofing (also known as DNS cache poisoning) is a cyberattack where an attacker injects false DNS records into a DNS resolver's cache. This causes the resolver to redirect legitimate traffic for a particular domain to a malicious server controlled by the attacker, leading to potential data theft or the spreading of malware.
